export type TextObject = {
    primary: string;
    secondary: string;
};
export type InputObject = {
    border: string;
    text: string;
    placeholder: string,

};
export type ColorScheme = 'dark' | 'light';
